# ──────────────────────────────────────────────────────────
# RigVision-3D — CV Pipeline Dependencies
# ──────────────────────────────────────────────────────────
# Install order:
#   1. PyTorch with CUDA (RTX 4070):
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   2. Everything else:
#      pip install -r requirements.txt
# ──────────────────────────────────────────────────────────

# Core
opencv-python>=4.8.0
numpy>=1.24.0
redis>=5.0.0

# Object Detection — YOLOv8
ultralytics>=8.0.0

# NOTE: the CV pipeline is now a single in-process orchestrator and no longer uses
# Kafka — detection → tracking → cross-camera → triangulation → zone fusion all run
# in one process and write Redis directly. (Kafka remains a backend-only concern for
# the diagnostics alert bus.)

# Tracking — BoT-SORT native (no boxmot wrapper)
lap>=0.4.0                # Jonker-Volgenant linear assignment (fast)
scipy>=1.11.0             # Fallback assignment + Kalman math + cosine distance
